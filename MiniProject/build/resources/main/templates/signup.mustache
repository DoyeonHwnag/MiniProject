{{>layouts/header}}
<main class="d-flex justify-content-center align-items-center" style="margin-top: 50px;">
    <div class="container" style="max-width: 500px;">
        <h2 class="text-center mb-4">회원가입</h2>
        <form action="/signup" method="post" class="p-4 shadow rounded" style="background-color: #f8f9fa;">
            <div class="mb-3">
                <label for="username" class="form-label">사용자명:</label>
                <input type="text" id="username" name="username" class="form-control" required>
            </div>

            <div class="mb-3">
                <label for="password" class="form-label">비밀번호:</label>
                <input type="password" id="password" name="password" class="form-control" required>
            </div>

            <div class="mb-3">
                <label for="adminCode" class="form-label">관리자 코드 (일반 사용자는 입력하지 마세요):</label>
                <input type="password" id="adminCode" name="adminCode" class="form-control">
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary">회원가입</button>
            </div>
        </form>

        <div class="text-center mt-3">
            <p>이미 계정이 있으신가요? <a href="/login">로그인</a></p>
        </div>
    </div>
</main>
{{>layouts/footer}}
